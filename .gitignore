# Dependencies
node_modules/

# Python caches
__pycache__/
*.pyc

# Secrets — API keys are read from the environment / ~/.env, never from the repo
.env
.env.local

# Vendored sparring-partner: personal calibration profile — never shipped
skills/sparring-partner/references/profile.md

# OS / editor cruft
.DS_Store
Thumbs.db
